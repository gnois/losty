-- call in init_worker_by_lua
ngx.update_time()
-- ngx.now() * 1000 gives millisecond precision; adding worker.pid() makes each worker distinct
-- Avoids float manipulation and is non-zero as long as nginx has been running (pid > 0)
math.randomseed(ngx.now() * 1000 + ngx.worker.pid())
